/**
 * Next.js Type configuration.
 * Defines the NextConfig type for the application.
 */
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
